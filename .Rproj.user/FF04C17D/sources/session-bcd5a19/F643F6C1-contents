# Markdown Syntax

Plain text.

*Italicized text*

**Bolded Text**

H~2~SO~4~

Fe^2+^

```Inline Code```

`Inline Code`

NORMAL CAPS

<span style="font-variant:small-caps;"> Small Caps </span> (does not format when knitting, but does when building ?)

[bioinformatics.ca youtube]:(https://www.youtube.com/@bioinformaticsdotca)
[This is an empty hyperlink, nothing in the brackets, so it is essentially the closest thing R has to a comment!]:()

![alt text or image title](bookdown-demo_files/figure-html/nice-fig-1.png)

^[Footnote!]

- list
  - list
- list
  - list

1. list point 1
2. list point 2
  - list

> "I thoroughly disapprove of duels. If a man should challenge me,
  I would take him kindly and forgivingly by the hand and lead him
  to a quiet place and kill him."
>
> --- Mark Twain

$f(k) = {n \choose k} p^{k} (1-p)^{n-k}$

\begin{equation} 
\begin{split}
\mathrm{Var}(\hat{\beta}) & =\mathrm{Var}((X'X)^{-1}X'y)\\
 & =(X'X)^{-1}X'\mathrm{Var}(y)((X'X)^{-1}X')'\\
\end{split}
(\#eq:var-beta)
\end{equation} 

\begin{equation} 
\begin{split}
\mathrm{Var}(\hat{\beta}) & =\mathrm{Var}((X'X)^{-1}X'y)\\
 & =(X'X)^{-1}X'\mathrm{Var}(y)((X'X)^{-1}X')'\\
\end{split}
(\#eq:var-alpha)
\end{equation} 

Note:
```
\begin{split} and \end{split} spread the equation name (default "Chapter #.x") across the lines of eqns
```

::: {.example}
This is a `theorem` environment that can contain **any**
_Markdown_ syntax.
:::

::: {.theorem name="Pythagorean theorem"}
For a right triangle, if $c$ denotes the length of the hypotenuse
and $a$ and $b$ denote the lengths of the other two sides, we have
$$a^2 + b^2 = c^2$$
:::

A normal paragraph.

(ref:foo) A scatterplot of the data `cars` using **base** R graphics. 

```{r foo, fig.cap='(ref:foo)'}
plot(cars)  # a scatterplot
```

Chapter \@ref(goober)

# Preface {#goober}



  

